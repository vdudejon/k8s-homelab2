apiVersion: awx.ansible.com/v1beta1
kind: AWX
metadata:
  name: awx
spec:
  service_type: clusterip
  ingress_type: ingress
  ingress_hosts:
    - hostname: aws.apps.k8s.vdude.io
      tls_secret: aws-cert